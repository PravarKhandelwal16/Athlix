# ────────────────────────────────────────────────────────────────────────────
# Athlix AI Backend — Python Dependencies
# ────────────────────────────────────────────────────────────────────────────
# Install:  pip install -r requirements.txt
# Tested on Python 3.10 / 3.11

# ── API Framework ────────────────────────────────────────────────────────────
fastapi==0.111.0
uvicorn[standard]==0.29.0     # ASGI server with WebSocket support
python-multipart==0.0.9       # Required for UploadFile / multipart form data

# ── Pose Detection ───────────────────────────────────────────────────────────
mediapipe>=0.10.30             # BlazePose (33-keypoint full-body model)
opencv-python-headless>=4.9.0.80  # OpenCV without GUI libs (server-safe)

# ── Numeric / ML Utilities ───────────────────────────────────────────────────
numpy>=1.26.4
scikit-learn>=1.4.2            # Pre/post-processing helpers
xgboost>=2.0.3                 # Gradient-boosted tree model (future integration)
joblib>=1.4.0                  # Model serialisation / deserialisation
shap>=0.45.1                   # Model explainability (SHAP values for tree models)

# ── Data Handling ────────────────────────────────────────────────────────────
pandas>=2.2.2                  # Data manipulation for training pipeline

# ── Validation ───────────────────────────────────────────────────────────────
pydantic>=2.7.1                # Comes bundled with FastAPI; pinned for stability

# ── Development / Debugging (optional — remove in production images) ──────────
# httpx==0.27.0                # Async HTTP client used in tests
# pytest==8.2.0
# pytest-asyncio==0.23.6
